"""ViperScan — know every device on the network you're sitting on.

A zero-dependency (stdlib-only) LAN scanner that discovers every device on the
local network, identifies its vendor and likely type, and flags the ones worth
a second look: cameras, surveillance/DVRs, voice assistants, hidden hosts,
exposed admin panels, and anything it simply can't identify.

Drop it on any machine with python3 and run it. No pip install required.
"""

__version__ = "1.0.0"
__app__ = "ViperScan"
